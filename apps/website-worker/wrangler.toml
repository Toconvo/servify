name = "servify-website"
main = "src/worker.ts"
compatibility_date = "2025-01-01"

# Serve static assets from the website source (apps/website)
[assets]
directory = "../website"
binding = "ASSETS"

[vars]
# Example: set additional environment variables if needed

# Optional: bind to a custom domain via routes (uncomment & set your zone_id)
# routes = [
#   { pattern = "www.example.com/*", zone_id = "YOUR_ZONE_ID" }
# ]

